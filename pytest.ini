[pytest]
asyncio_mode = auto
# Both fixture and test event loops use module scope so the integration
# conftest's explicit loop_scope="module" fixtures and test functions share
# the same event loop without mismatch (#806).
asyncio_default_fixture_loop_scope = module
asyncio_default_test_loop_scope = module
testpaths = tests
addopts = -v --tb=short
