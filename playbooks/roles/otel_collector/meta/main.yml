---
galaxy_info:
  role_name: otel_collector
  description: Installs and configures otelcol-contrib (prometheus receiver -> OTLP exporter push), systemd on Linux / launchd on macOS.
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: EL
      versions:
        - all
    - name: Debian
      versions:
        - all
    - name: Ubuntu
      versions:
        - all
    - name: MacOSX
      versions:
        - all

dependencies: []
