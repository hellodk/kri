---
# playbooks/roles/salt_minion/tasks/install_redhat.yml
# Install salt-minion on RHEL/Rocky/Alma from the bundled onedir tarball
# (AIR-GAPPED, #970) — no dnf repo / internet access required on the target.
# Dispatched only when ansible_os_family == 'RedHat' (see tasks/main.yml).

- name: Install salt-minion from bundled onedir tarball (RHEL/Rocky)
  include_tasks: install_linux_onedir.yml
