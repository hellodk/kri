---
# playbooks/roles/salt_minion/tasks/install_debian.yml
# Install salt-minion on Debian/Ubuntu from the bundled onedir tarball
# (AIR-GAPPED, #970) — no apt repo / internet access required on the target.
# Dispatched only when ansible_os_family == 'Debian' (see tasks/main.yml).

- name: Install salt-minion from bundled onedir tarball (Debian/Ubuntu)
  include_tasks: install_linux_onedir.yml
