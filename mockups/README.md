# UI mockups

Standalone, self-contained HTML mockups kept for design reference. They are not
part of the build or served by the app — open them directly in a browser.

| File | Describes |
| --- | --- |
| `node-health-rollup.html` | The unified node **health** rollup badge (worst-of Salt presence + SSH) with the light hover tooltip, shipped in PR #850. Shows the before/after and every health state. |
